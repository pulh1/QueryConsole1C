# Проверка объединённой реализации metadata provider

Дата проверки: 2026-09-02.

## Результат

Семантика и UI собраны в одной ветке `feature/metadata-provider`. Реестр
маршрутизирует точечный поиск таблиц и полей к одному экземпляру каждого
поставщика, а UI строит каталог через тот же реестр и раскрывает узлы лениво.
Временные таблицы и вложенные запросы остаются локальными источниками текущего
разбора и не превращены в глобальных поставщиков.

Функциональный acceptance пройден. Единственная ошибка полного YAxUnit-прогона
воспроизводится на чистом `master` и связана с языком текста платформенного
исключения. Три пропуска также не созданы этой веткой.

## Границы дизайна

- Реестр создаётся один раз для корневого разбора и передаётся в производные
  контексты; поставщики и их кэши живут столько же.
- Стандартный поставщик хранит статический список первых сегментов и не
  обходит `СхемаЗапроса.ДоступныеТаблицы` при инициализации.
- Семантика использует `НайтиТаблицу()` и `НайтиПоле()`; полное описание
  таблицы не строится в горячем точечном поиске.
- Полное нейтральное описание создаётся только там, где оно действительно
  нужно потребителю, в том числе для параметров исполняемого представления.
- Каталог UI получает корни и дочерние элементы через реестр. Изменений XML
  форм (`Form.form`) нет.
- `ИсточникДанныхВременнаяТаблица` разрешается по локальному описанию ВТ.
  `ИсточникДанныхВложенныйЗапрос` читает колонки вложенной модели. Остальные
  именованные таблицы маршрутизируются через реестр.
- Имена production-объектов не получили тестовый префикс `КОНС_`; он сохранён
  только в тестовом проекте YAxUnit согласно существующей схеме именования.

## Выполненные проверки

### YAxUnit

Фокусные suites после исправления замечаний независимого review:

- поставщики и реестр: 44/44;
- семантика, исполняемые представления и построение/генерация: 46/46
  (`30 + 9 + 7`);
- поставщики и прикладные потребители совместно: 65 passed из 67, два
  известных headless skip из-за
  отсутствия `FindByID` у `ДеревоЗначений`;

Полный прогон: 615 тестов, 611 passed, 1 failed, 3 skipped. Ошибка
`КОНС_Обр_Парсер_МО.НеизвестныйУзелВыраженияВызываетИсключениеГенератораТекста`
ожидала русский текст исключения, а платформа вернула английский. Точный тест
повторён на чистом `master` с тем же результатом.

Отчёты свежих прогонов EDT-MCP:

- 46/46:
  `C:\Users\pkhlu\AppData\Local\Temp\edt-mcp-yaxunit\QueryConsoleZUP_Benchmark_______________282da3d3310e3faea2feac2196be07dc7e3d6ec1\report.md`;
- 65 passed, 2 skipped:
  `C:\Users\pkhlu\AppData\Local\Temp\edt-mcp-yaxunit\QueryConsoleZUP_Benchmark_______________66942b5e21aabd26a3dc7dce2d041500ce979972\report.md`;
- полный прогон:
  `C:\Users\pkhlu\AppData\Local\Temp\edt-mcp-yaxunit\QueryConsoleZUP_Benchmark_______________f5bb5924d9414cdb35a8d17172ae7fa38abd8c91\report.md`.

### Vanessa MCP

В реальном test client успешно выполнены 6 из 6 релевантных сценариев:

- три сценария нового файла `КаталогПоставщиковМетаданных.feature` —
  `Success`, 4 602 ms, 188 шагов;
- существующий сценарий без ВТ `ФизЛица` — `Success`, 6 169 ms, 239 шагов;
- существующий сценарий параметров среза последних — `Success`, 15 459 ms,
  638 шагов;
- существующий сценарий с ВТ `ФизЛица` — `Success`, 8 291 ms, 335 шагов.

Новая feature прошла `open_feature_file` и `check_syntax` без ошибок. Прямой
результат `run_scenario` подтверждался немедленным `get_test_results` после
каждого файла. Последующий state Vanessa иногда показывал устаревшее «не
запускался», поэтому он не использован вместо результата операции. Появлявшийся
диалог обновления тестовой ИБ был окном EDT, а не Vanessa.

### EDT и статические инварианты

- основной EDT-проект переключён на commit ветки и импортирован;
- выполнено инкрементальное обновление тестовой ИБ;
- активных EDT debug launch не было;
- `.form` в diff отсутствуют;
- в семантическом hot path отсутствуют вызовы полного `ОписаниеТаблицы()` и
  `ТаблицаСуществует()`;
- `git diff --check` проверяется с учётом допустимого CRLF в сохранённых JSON
  benchmark artifacts.

EDT содержит существующий фон диагностик. Уменьшение или отсутствие новых
ошибок не трактуется как полностью чистый проект; блокирующих диагностик,
связанных с новым контрактом, не обнаружено. Общий snapshot EDT на момент
финальной проверки: 1291 marker, включая 36 `ERRORS`, 4 `BLOCKER`, 1
`CRITICAL`, 169 `MAJOR`, 1080 `MINOR` и 1 `TRIVIAL`; это фон всего проекта, а
не delta данной ветки. Для затронутых объектов использовалось сравнение с
исходным состоянием, а не требование нулевого общего числа marker.

## Производительность

Сравнение от 2026-08-31 аннулировано после аудита provenance: восемь
feature-sidecar не соответствовали заявленному source commit, а восемь
baseline-sidecar содержали неверный hash semantic visitor. Вся серия из 16
JSON удалена. Подробности и точные несовпавшие хэши приведены в
`docs/research/2026-08-31-real-query-semantic-benchmark.md`. Старые проценты и
PASS gate больше не используются как доказательство old/new.

Взамен выполнена полностью новая контрбалансированная серия `F–B–B–F` с
проверяемым provenance обеих реализаций. Подробный отчёт:
`docs/research/2026-09-02-metadata-provider-unified-benchmark.md`.
Каждый запуск выполнял только регистрацию
`КОНС_Обр_БенчмаркПарсера_МО.RuntimeBaselineСемантическогоКонвейераФормируется`.
Перед ним были остановлены Runtime MCP, RDBG и test client Vanessa; EDT
подтвердил ноль debug launch. Профилирование и counters в timed run не
включались.

| Корпус | Baseline median / p95, ms | Feature median / p95, ms | Ratio median / p95 |
| --- | ---: | ---: | ---: |
| `semantic_contract` | 14,50 / 22,00 | 17,00 / 26,00 | 1,1724 / 1,1818 |
| `repeated_standard_source` | 3,125 / 5,25 | 3,0625 / 4,5625 | 0,9800 / 0,8690 |
| `semantic_query_examples_all_42` | 687,50 / 836,00 | 682,50 / 786,00 | 0,9927 / 0,9402 |
| `semantic_large_package` | 72,00 / 103,00 | 74,00 / 100,00 | 1,0278 / 0,9709 |

Для корпуса 42 запросов нормализованная median одного запроса равна примерно
16,37 ms на baseline и 16,25 ms на feature. Это деление времени полного
прохода корпуса на 42, а не распределение индивидуальных задержек.

Четыре raw artifact и их SHA-256 перечислены в подробном benchmark-отчёте.
Каждый sidecar скопирован без изменения байтов. Проверены schema 2, 5 или 10
artifact rows и их хэши, четыре корпуса, порядок и идентичность входов,
3 warmup, 20 положительных samples на запуск и platform `8.3.27.2170`.
EDT-MCP переиспользовал каталог JUnit для одинакового exact-вызова; итоговый
отчёт последнего запуска:
`C:\Users\pkhlu\AppData\Local\Temp\edt-mcp-yaxunit\QueryConsoleZUP_Benchmark_______________d1ea4c5826d9f8585074621b8d8fc6ddd5852ab1\report.md`.

Для каждой реализации объединено 40 raw samples. Все четыре sidecar имеют
одинаковые corpus/input identity, runtime и методику; artifact hash совпадают с
заявленными commit. На реальном корпусе из 42 запросов и большом пакете
регрессии полного конвейера не обнаружено. Controlled contract показывает
накладные расходы на коротких запросах; micro-корпус repeated source имеет
разный calibrated batch 8/16 и используется только как вспомогательный сигнал.

## Известные ограничения

- Интеграционные UI-тесты подтверждают пользовательский каталог и основные
  параметры, но не заменяют проверку каждого варианта прикладных данных.
- Два headless YAxUnit-теста формы остаются пропущенными из-за различия
  `ДеревоЗначений` и данных формы.
- Runtime MCP был подключён к другой ИБ и не использован как feature
  acceptance; подробности вынесены в отдельный runtime-отчёт.
