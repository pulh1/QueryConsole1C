# Unit-тесты полного парсера языка запросов

**Дата:** 2026-08-04
**Статус:** проект после второго review
**Scope:** black-box YAxUnit для `Обработки.Парсер.Разобрать`

## Цель и границы

Новый серверный модуль `КОНС_Обр_ПарсерЗапросов_МО` проверяет полный текст
запроса через публичный `Парсер.Разобрать(Текст)`. Production parser, grammar,
`ЭлементыМоделиЗапроса`, semantic visitor и данные информационной базы не
изменяются и не вызываются.

В production-диапазоне `ObjectModule.bsl:236-1708` объявлено 69 query-функций:
67 входят в reachable alternative inventory. Две функции исключены по
production call graph: `НеТерминалКакОпционально` на строке 1001 не имеет
caller, а `НеТерминалВыражениеСКДПараметр` на строке 1117 вызывается только
самой собой. Обе имеют статус `excluded: unreachable from Разобрать`,
проверяются в Task 0 и не подменяются фиктивными black-box cases.

Тесты фиксируют сырой AST. В частности, `ПОМЕСТИТЬ` и `РАЗРЕШЕННЫЕ` проверяются
на том raw path, который подтвердит runtime-preflight; semantic-перенос в поля
`ЗапросВыбора` вне scope. Списки проверяются по значениям и порядку, но не по
праворекурсивной topology parser.

## Точки истины

Task 0 читает:

- `Парсер/ObjectModule.bsl:236-1708` и FIRST-table;
- `ЭлементыМоделиЗапроса/Module.bsl:57-601`, включая factories выражений
  462–601 и `НовыйВыражениеМоделиЗапроса`;
- существующие YAxUnit-модули parser и future grammar;
- 42 `QueryExamples/*.q1c`.

Проверенные factory values нельзя заменять придуманными узлами:

- JOIN type — строки `Левое`, `Правое`, `Внутреннее`, `Полное`;
- UNION type — `Объединить` или `ОбъединитьВсе`;
- ORDER direction — `Возр` или `Убыв`;
- control-point type — `Иерархия` или `ТолькоИерархия`;
- WHERE/HAVING/JOIN conditions — wrapper `ВыражениеМоделиЗапроса`, выражение
  находится в `.Значение`.

## Exact alternative inventory

Обозначения: `ε` — ветвь отсутствия optional production; case IDs определены в
implementation plan. Одна строка перечисляет все ветви конкретной функции, а
несколько ветвей могут покрываться одним составным case.

| # | Production (line) | Все альтернативы | Case IDs / coverage evidence |
|---:|---|---|---|
| 1 | `ПакетЗапросов` (236) | `ЗапросПакета Продолжение` | Q00–Q05 |
| 2 | `ПродолжениеПакетаЗапросов` (250) | `; ЗапросПакета ...`; конечный `;`; `ε` | Q01; Q02; Q00 |
| 3 | `ЗапросПакета` (274) | SELECT; УНИЧТОЖИТЬ | Q00; Q03 |
| 4 | `ЗапросУничтожения` (299) | `УНИЧТОЖИТЬ ID` | Q03 |
| 5 | `ЗапросВыбора` (313) | оператор + UNION/ORDER/INDEX/TOTALS | Q00, C04, C05, T02, T03 |
| 6 | `ОбъединяемыйЗапрос` (337) | SELECT operator | Q00 |
| 7 | `БлокОбъединить` (371) | `ОБЪЕДИНИТЬ [ВСЕ]`; `ε` | C04, C05; Q00 |
| 8 | `ВсеОпционально` (398) | `ВСЕ`; `ε` | C05; C04 |
| 9 | `БлокВыбрать` (412) | `ВЫБРАТЬ Модификаторы` | Q00, M01–M15 |
| 10 | `МодификаторыВыборки` (424) | starts `ПЕРВЫЕ`; starts `РАЗЛИЧНЫЕ`; starts `РАЗРЕШЕННЫЕ`; `ε` | M01/M04–M07; M02/M08–M11; M03/M12–M15; Q00 |
| 11 | `РазличныеРазрешенныеОпционально` (461) | `РАЗЛИЧНЫЕ [РАЗРЕШЕННЫЕ]`; `РАЗРЕШЕННЫЕ [РАЗЛИЧНЫЕ]`; `ε` | M04/M06; M05/M07; M01 |
| 12 | `РазрешенныеОпционально` (488) | `РАЗРЕШЕННЫЕ`; `ε` | M06/M10; M04/M08 |
| 13 | `ПервыеРазрешенныеОпционально` (505) | `ПЕРВЫЕ [РАЗРЕШЕННЫЕ]`; `РАЗРЕШЕННЫЕ [ПЕРВЫЕ]`; `ε` | M08/M10; M09/M11; M02 |
| 14 | `ПервыеОпционально` (533) | `ПЕРВЫЕ`; `ε` | M11/M15; M09/M13 |
| 15 | `ПервыеРазличныеОпционально` (550) | `РАЗЛИЧНЫЕ [ПЕРВЫЕ]`; `ПЕРВЫЕ [РАЗЛИЧНЫЕ]`; `ε` | M13/M15; M12/M14; M03 |
| 16 | `ПоляВыборки` (578) | первое поле + continuation | Q00, F01 |
| 17 | `СписокПолейОпционально` (593) | `, Поле ...`; `ε` | F01; Q00 |
| 18 | `ПолеВыборки` (613) | expression + alias; `*` | Q00/F02/F03/F05/F06; F04 |
| 19 | `Псевдоним` (645) | `КАК ID`; bare `ID` | F02/S09; F03/S10 |
| 20 | `ПсевдонимОпционально` (671) | alias; `ε` | F02/F03/S09/S10; Q00/S01 |
| 21 | `БлокПоместить` (687) | `ПОМЕСТИТЬ ID`; `ε` | T01; Q00 |
| 22 | `БлокИз` (705) | `ИЗ Источник ...`; `ε` | S01; Q00 |
| 23 | `СписокТаблиц` (722) | `, Источник ...`; `ε` | S07; S01 |
| 24 | `ИсточникДанныхЗапроса` (738) | source + joins | S01, J01 |
| 25 | `Соединение` (754) | обычный JOIN; `{ optional JOIN }`; `ε` | J01–J04/J06/J07; J05; S01 |
| 26 | `ПраваяЧастьСоединения` (784) | type + source + `ПО` condition | J01–J07 |
| 27 | `ИсточникДанныхСоединения` (808) | joinable source + nested joins | J01–J07 |
| 28 | `ТипСоединения` (824) | ЛЕВОЕ; ПРАВОЕ; ВНУТРЕННЕЕ; ПОЛНОЕ | J01; J02; J03; J04 |
| 29 | `ИсточникДанных` (855) | table value; metadata table; nested query; temporary table | S05; S01/S09/S10; S04; S06 |
| 30 | `ПрисоединяемаяТаблица` (894) | metadata table; nested query; temporary table | J01; J07; J06 |
| 31 | `ИсточникДанныхТаблицаЗначений` (926) | parameter + required alias | S05 |
| 32 | `ИсточникДанныхВременнаяТаблица` (943) | ID + optional alias | S06 |
| 33 | `ИсточникДанныхТаблица` (960) | dotted ID + optional params/alias | S01–S03/S08–S10 |
| 34 | `ИсточникДанныхВложенныйЗапрос` (982) | `(ЗапросВыбора)` + required alias | S04/J07 |
| 35 | `РазыменованиеТаблицы` (1014) | first dotted component + continuation | S08 |
| 36 | `ПродолжениеРазыменованияТаблицы` (1029) | `.ID ...`; `ε` | S08; S01 |
| 37 | `ПараметрыТаблицыОпционально` (1048) | `(СписокПараметров)`; `ε` | S02/S03; S01 |
| 38 | `СписокПараметров` (1066) | first parameter + continuation | S02/S03 |
| 39 | `ПродолжениеСпискаПараметров` (1081) | `, Параметр ...`; `ε` | S03; S02 |
| 40 | `ПараметрТаблицы` (1101) | `ВыражениеМоделиЗапроса` | S02/S03 |
| 41 | `БлокГде` (1133) | `ГДЕ expression`; `ε` | C01; Q00 |
| 42 | `БлокСгруппировать` (1151) | `СГРУППИРОВАТЬ ПО list`; `ε` | C02/C12; Q00 |
| 43 | `БлокИмеющие` (1170) | `ИМЕЮЩИЕ expression`; `ε` | C03; Q00 |
| 44 | `БлокУпорядочить` (1188) | `УПОРЯДОЧИТЬ ПО list`; `ε` | C06–C10/C14; Q00 |
| 45 | `АвтоупорядочиваниеОпционально` (1207) | `АВТОУПОРЯДОЧИВАНИЕ`; `ε` | C11; Q00 |
| 46 | `СписокЭлементовУпорядочивания` (1221) | first ORDER element + continuation | C06/C10 |
| 47 | `ПродолжениеСпискаЭлементовУпорядочивания` (1235) | `, element ...`; `ε` | C10; C06 |
| 48 | `ЭлементУпорядочивания` (1254) | expression + direction + hierarchy | C06–C10 |
| 49 | `НаправлениеУпорядочивания` (1275) | explicit `ВОЗР`; `УБЫВ`; `ε` default | C07; C08; C06 |
| 50 | `ИерархияОпционально` (1296) | `ИЕРАРХИЯ`; `ε` | C09; C06 |
| 51 | `БлокИтоги` (1310) | `ИТОГИ fields ПО points`; `ε` | T03–T20; Q00 |
| 52 | `ПоляИтогов` (1334) | first total field + continuation | T03/T04 |
| 53 | `КонтрольныеТочкиИтогов` (1352) | first point + continuation | T03/T05 |
| 54 | `СписокКонтрольныхТочекОпциональное` (1366) | `, point ...`; `ε` | T05; T03 |
| 55 | `КонтрольнаяТочкаИтогов` (1385) | expression point; `ОБЩИЕ` | T03; T06 |
| 56 | `ПродолжениеКонтрольнойТочкиИтогов` (1411) | optional type + periods + alias | T07–T20 |
| 57 | `ДополнениеПериодамиИтогов` (1429) | `ПЕРИОДАМИ(...)`; `ε` | T11–T20; T03 |
| 58 | `ТипКонтрольнойТочки` (1450) | `ТОЛЬКО ИЕРАРХИЯ`; `ИЕРАРХИЯ`; `ε` | T08; T07; T03 |
| 59 | `ПсевдонимКонтрольнойТочкиИтогов` (1470) | `КАК ID`; bare `ID`; `ε` | T09; T10; T03 |
| 60 | `ТипПериодаИтогов` (1494) | СЕКУНДА; МИНУТА; ЧАС; ДЕНЬ; НЕДЕЛЯ; МЕСЯЦ; ГОД; ДЕКАДА; ПОЛУГОДИЕ | T11–T19 соответственно |
| 61 | `ПериодДополненияИтогов` (1568) | `, start [, end]`; `ε` | T20; T11 |
| 62 | `ОкончаниеПериодаИтогов` (1590) | `, end`; `ε` | T20; T11 |
| 63 | `ПериодИтогов` (1607) | expression | T20 |
| 64 | `БлокИндексировать` (1623) | `ИНДЕКСИРОВАТЬ ПО list`; `ε` | T02; Q00 |
| 65 | `РасширениеСКД` (1642) | `{ body }`; `ε` | K01–K05; Q00 |
| 66 | `ТелоБлокаСКД` (1657) | SELECT/ORDER/TOTALS field body; `ГДЕ` field body | K01–K03; K04 |
| 67 | `ТипБлокаСКД` (1685) | `ВЫБРАТЬ`; `УПОРЯДОЧИТЬ ПО`; `ИТОГИ ПО` | K01; K02; K03 |
| — | `КакОпционально` (1001) | declaration exists, caller absent | excluded |
| — | `ВыражениеСКДПараметр` (1117) | only self-recursion, no external caller | excluded |

Task 10 переносит эту таблицу в фактическую coverage matrix и дробит строку на
отдельные alternatives. Ни одна ветвь, включая `ε`, не получает общий статус
без ссылки на case.

## Exact synthetic budget

Case — один вызов `.СПараметрамиНаСервере(...)` либо один
непараметризованный тест. Bootstrap discovery RED временный и не считается.

| Task | IDs | Cases |
|---|---|---:|
| 1 smoke | Q00 | 1 |
| 2 package/destroy | Q01–Q05 | 5 |
| 3 modifiers/fields/alias error | M01–M15 (11 GREEN + 4 opt-in RED), F01–F07, N-ALIAS | 23 |
| 4 sources/parameters | S01–S10 | 10 |
| 5 JOIN | J01–J07 | 7 |
| 6 clauses/UNION/ORDER | C01–C15 | 15 |
| 7 PLACE/INDEX/TOTALS | T01–T21 | 21 |
| 8 SKD | K01–K06 | 6 |
| 9 errors/reuse | E01–E04 | 4 |
| **Synthetic** |  | **1+5+23+10+7+15+21+6+4 = 92** |
| Corpus | X01–X42 | 42 |
| **Overall target** |  | **134** |

M01–M15 — все непустые перестановки без повторов множества
`ПЕРВЫЕ/РАЗЛИЧНЫЕ/РАЗРЕШЕННЫЕ`; Q00 закрывает пустую ветвь. Из этих пятнадцати
альтернатив M01–M05, M08, M09, M11–M13 и M15 являются GREEN в основном модуле,
а подтверждённые runtime-блокеры M06, M07, M10 и M14 представлены только
opt-in RED cases существующего future-grammar модуля. Поэтому inventory
представляет все 15 альтернатив, но ни одна из четырёх неисправных альтернатив
не помечена ложно GREEN. Task 3 сохраняет бюджет `11 + 4 + 7 + 1 = 23`;
синтетический бюджет остаётся 92, из них 88 GREEN в основном модуле и четыре
opt-in RED. После 42 corpus cases общий represented target `134` состоит из
`130` GREEN cases основного модуля и четырёх opt-in modifier RED. T11–T19 — девять
типов периода в production-порядке. C07 содержит явное `ВОЗР`, C06 — default
`ε`, C08 — `УБЫВ`. S09/S10 — полезные достижимые cases явного
`Каталог.Таблица КАК Т` и bare `Каталог.Таблица Т`; они не используются для
двух исключённых productions. J07 — двух-JOIN chain с вложенным первым правым
источником и обычным вторым.

F05 (`ВЫБРАТЬ Т.* ИЗ Таблица КАК Т`) проверяет nested source-all-fields
boundary, а не leaf type на корне. Первый main run дал
`25 total / 24 passed / 1 failed`, report
`8b1dc2a30_0603d958d35f2ab1215dde45c14f531d32933588`; единственный F05 failure
показал фактический root `Разыменование`. Exact conditional debug evidence
`1785806808318-2` подтвердил полный public AST:
`Поле.Выражение.Тип=ВыражениеМоделиЗапроса`,
`Поле.Выражение.Значение.Тип=Разыменование`, два элемента разыменования,
строковый `[0]="Т"` и `[1].Тип=ВыражениеВсеПоляИсточника`. Production parser
создаёт `Разыменование` и добавляет leaf в `Элементы`; factories создают эти два
разных shape, а генератор текстов потребляет `ВыражениеВсеПоляИсточника` именно
внутри `Разыменование.Элементы`. Поэтому F01–F07 используют явный шестой
параметр `ТипПоследнегоЭлементаРазыменования`: только F05 передаёт
`"ВыражениеВсеПоляИсточника"`, остальные — `Неопределено`; F05 ожидает root
`"Разыменование"`, count `2` и тип последнего элемента. Case arithmetic не
меняется.

## Runtime-preflight и observability

Task 0 строго read-only. После создания test module Task 1A выполняет
ограниченный runtime spike для спорных source classifications, всех modifier
orders, J06/J07 JOIN topology и СКД. Он создаёт verified artifact с exact input,
result/error, raw AST path/value, report ID и generated BSL literal. Для J07
metadata-источник должен быть dotted (`Каталог.Таблица3`), потому что bare
одночастный идентификатор является временной таблицей. Remediation artifact R1
обязан подтвердить два элемента `Корень.Соединения[0]` и `[1]`, а
правые источники — `Оператор.Источники.Элементы[1]` и `[2]`; J06 подтверждает
один JOIN того же корня и временный правый источник. Artifact содержит ровно
44 строки: 15 M, 10 S, 2 J, 10 T-period, 6 K и одну E02. Помимо двух bootstrap
writes Task 1, только bounded
spikes Task 1A, Task 8A и corpus count/wiring Task 9B имеют право на две guarded
EDT writes. Tasks 1A/8A добавляют и удаляют probes; Task 9B сначала собирает
runtime counts, затем заменяет probes окончательными 42 cases. Tasks 2, 4–7,
8B и 9 имеют по одной guarded write. Task 3 имеет ровно по одной guarded write
на каждый из двух существующих модулей — основной full-query и opt-in
future-grammar (две writes total, без нового metadata object). Independent
review round 1 отдельно разрешает
Task 1A-R1 ещё две guarded writes для 17 уникальных exact-case probes и полного
восстановления Task 1; это разрешение не меняет counts `92 + 42 = 134`.
После подтверждённого F05 failure отдельный Task 3-R1 разрешает ровно одну
дополнительную guarded write только основного full-query модуля для исправления
test contract. Это разрешение не распространяется на future-grammar module и
не допускает его повторный run; parser, factories, consumers и grammar остаются
read-only.

Для `ТипБлокаСКД` нельзя считать проверкой сравнение только
`Пакет.Тип` или использование expected keyword в сообщении. Artifact должен
дать наблюдаемый raw AST path, различающий `ВЫБРАТЬ`, `УПОРЯДОЧИТЬ ПО` и
`ИТОГИ ПО`. Если production parser не экспортирует такой результат, Task 8
останавливается с `observability gap`; private method не вызывается и coverage
не помечается GREEN.

## Негативные границы

Отдельные cases обязательны:

- N-ALIAS: `ВЫБРАТЬ 1 КАК`;
- C15: `ВЫБРАТЬ 1 УПОРЯДОЧИТЬ ПО`;
- T21: `ВЫБРАТЬ 1 ИТОГИ ПО`;
- K06: exact незавершённый СКД-блок из runtime artifact.

Используется существующий API
`.Метод("Разобрать", ЮТМетоды.МассивПараметров(...)).ВыбрасываетИсключение(...)`.

## Corpus handoff

Task 9A формирует reproducible source artifact с 42 четырёхаргументными BSL
registration lines для runtime-count probes; Task 9B детерминированно добавляет
пятым аргументом проверенный count для окончательных cases. Для каждого первого
`<query>` хранятся отдельно:

- relative filename key с `/`;
- XML `name`;
- SHA-256 decoded UTF-8 text;
- escaped BSL expression;
- позднее `RuntimeVerifiedCount` и YAxUnit report ID.

XML entities декодируются XML parser до checksum/escaping. В BSL каждая `"`
удваивается до `""`; LF представляется явным `+ Символы.ПС +`; пустые строки и
tabs сохраняются; `&` остаётся decoded `&`. Parser-independent алгоритма,
который надёжно считает элементы пакета при комментариях, строковых литералах и
вложенных запросах, нет, поэтому pre-wire count не вычисляется. Первая guarded
write Task 9B запускает 42 временных runtime-count probes после wiring и
сохраняет counts из контролируемых сообщений. Вторая write заменяет probes
окончательными 42 cases с уже проверенным `RuntimeVerifiedCount`.

## Future grammar

Существующий opt-in `КОНС_Обр_ПарсерБудущаяГрамматика_МО` сохраняет явный
module-filter gate без изменений. К трём существующим RED (`--1`, имя функции
`НЕДЕЛЯ(&Дата)` и сообщение ошибки для `1 +`) Task 3 добавляет один
параметризованный full-query modifier acceptance method с четырьмя desired
contracts: M06/M07/M10/M14 ожидают `ПЕРВЫЕ=5`, `РАЗЛИЧНЫЕ=Истина` и
`РАЗРЕШЕННЫЕ=Истина`. Runtime artifact
`docs/superpowers/matrices/2026-08-04-query-full-parser-runtime-preflight.md`
фиксирует фактическую границу:

- M06/M10 остаются runtime errors `Поле объекта не обнаружено
  (ВыбиратьРазрешенные)` — reports
  `696aabbdb_543b4d259945c4b680c30f7339fac3665b8fc262` и
  `5b86b1cc0_8912c057bb228f05ce83c2d39adedd835b733d32`;
- M07/M14 остаются assertion failures из-за
  `ВыбиратьРазличные=Ложь` — reports
  `d8cfe1f1f_a05525b9f125c94d661adc63d2b86b36f58272aa` и
  `e522eafb6_f12ad5227705b0f2306214b2959313f133871e52`.

Точный opt-in module run ожидает `7 total / 0 passed / 4 failed / 3 errors`.
Основной модуль не регистрирует эти четыре blockers; его narrow Task 3 run
ожидает `25/25` после Q00, Q01–Q05, 11 GREEN M, 7 F и N-ALIAS. Parser и grammar
остаются read-only.

Фактический opt-in result уже подтверждён report
`725d8a737_36b73a2a69d631547d3c684d8ed3e380dfdcbf45`:
`7 total / 0 passed / 4 failed / 3 errors`. Task 3-R1 сохраняет его без новой
future-grammar write и без повторного запуска; после единственной main-module
write ожидается только exact incremental main rerun `25/25`.

## Инкрементальность и готовность

Все YAxUnit launches используют только `updateBeforeLaunch=true` и
`updateScope="extension:yaxunit"`. `clean_project`, full rebuild, scope `all` и
запуск всех extensions запрещены.

Task 10 условно обновляет этот design при подтверждённом review fix, чтобы
арифметика и alternative inventory не расходились с test module. Независимый
reviewer, а не автор плана, принимает решение о готовности.
