## 1. Discovery And Metadata Setup

- [x] 1.1 Проверить через EDT-aware инструменты фактические объекты метаданных и создать новый объект обработки для реестра поставщиков метаданных таблиц. Проверка: объект создан EDT-aware способом, список объектов метаданных подтвержден, ручных правок `.mdo` нет.
- [x] 1.2 Прочитать экспортные методы `ПоставщикИсполняемыхПредставлений`, `ПоставщикИсполняемыхПредставленийПовтИсп`, `КонструкторЗапросовФормы`, `ПостроительМоделиЗапроса`, `ОбработкаПредставлениеЗапросов` и зафиксировать точки миграции. Проверка: список методов и ссылок сохранен в рабочем контексте реализации.
- [x] 1.3 Найти все прямые обращения к `ДоступныеТаблицы`, `ОписаниеПредставленияПоИмениТаблицы`, `ГруппыИсполняемыхПредставлений`, `ИменаДочернихГрупп` и `ИсполняемоеПредставление.`. Проверка: результаты поиска покрывают модель запроса, UI, семантику, генерацию кода и СКД.

## 2. Registry Contract And Data Shapes

- [x] 2.1 Создать контракт нейтральных описаний поставщика метаданных таблиц: сегменты, описание таблицы, поля, параметры, дочерние элементы UI. Трассировка: `metadata-provider-registry` requirements `Providers expose a shared metadata contract`, `Registry resolves table metadata providers by first name segment`. Проверка: BSL-модуль компилируется, структуры не содержат долгоживущих ссылок на уничтожаемую `СхемаЗапроса`.
- [x] 2.2 Реализовать фабрику реестра и индекс выбора поставщика по первому сегменту имени таблицы. Трассировка: design `Реестр поставщиков как объект обработки`, `Определение поставщика по первому сегменту`. Проверка: программный сценарий выбирает поставщика для `Справочник.*`, `РегистрСведений.*`, `ИсполняемоеПредставление.*` и отклоняет неизвестный сегмент.
- [x] 2.3 Добавить режимы жизненного цикла реестра для обработки запроса и UI формы. Трассировка: proposal про валидность `ДоступныеТаблицы`. Проверка: реестр не использует общий глобальный `СхемаЗапроса` и явно владеет состоянием экземпляра.
- [x] 2.4 Реализовать серверные методы формы или серверный общий модуль для доступа UI к реестру; клиентский код не должен получать объект реестра напрямую. Трассировка: requirement `Query constructor UI uses registry for available table hierarchy`. Проверка: клиентские вызовы идут через серверные методы.

## 3. Built-In Providers

- [x] 3.1 Реализовать поставщик стандартных таблиц 1С с собственным экземпляром `СхемаЗапроса` и безопасным доступом к `ДоступныеТаблицы`. Трассировка: requirement `Standard table provider owns query schema lifetime`. Проверка: поля стандартной таблицы читаются через живую схему или копируются в нейтральное описание.
- [x] 3.2 Реализовать поставщик исполняемых представлений как адаптер к `ПоставщикИсполняемыхПредставлений` без изменения обработок `Представление*`. Трассировка: requirement `Executable view provider remains compatible with existing view descriptions`. Проверка: существующее `ИсполняемоеПредставление.*` возвращает прежние поля, параметры, обработчик исполнения и генераторы.
- [x] 3.3 Реализовать контекстный поставщик временных таблиц на основе `ОписанияВременныхТаблиц` и индекса запроса пакета. Трассировка: requirement `Temporary tables are exposed through contextual provider`. Проверка: ВТ доступна после `ПОМЕСТИТЬ`, недоступна после уничтожения, поля ВТ раскрываются через реестр.
- [x] 3.4 Сохранить или обновить фасады публичных методов `ПоставщикИсполняемыхПредставлений` для совместимости. Трассировка: requirement `Existing public provider API remains available during migration`. Проверка: прямой вызов `ОписаниеПредставленияПоИмениТаблицы()` и методов групп возвращает совместимые результаты.

## 4. Query Model Integration

- [x] 4.1 Перевести `ПостроительМоделиЗапроса.ДобавитьИсточник()` с прямого создания `СхемаЗапроса` и fallback-а в `ПоставщикИсполняемыхПредставлений` на разрешение источника через реестр. Трассировка: requirement `Query model processing uses registry for source resolution`. Проверка: добавление стандартной таблицы, временной таблицы, таблицы значений и исполняемого представления сохраняет прежнее поведение.
- [x] 4.2 Перевести `ОбработкаПредставлениеЗапросов.ОбработатьИсточникЗапроса()` на получение описания исполняемого представления через реестр или совместимый адаптер. Трассировка: design `Использование в модели запроса и UI`. Проверка: параметры представления, `ВТФильтр` и отборы применяются как до рефакторинга.
- [x] 4.3 Перевести `ОбработкаМоделиЗапроса.КонтекстОбработкиВыражения()` на создание и передачу реестра или совместимого контекста метаданных вместе с текущими `ДоступныеТаблицыИБ` и `__СхемаЗапрос`. Трассировка: requirement `Semantic model processing uses registry-backed context`. Проверка: контекст обработки выражений сохраняет жизненный цикл стандартных таблиц и доступен всем этапам обработки модели.
- [x] 4.4 Проверить и при необходимости обновить `ОбработкаМоделиЗапроса.ОбработатьМодельЗапроса()`, `ОбработатьОператор()`, `ОбработатьПараметрыВиртуальнойТаблицы()` и `ОбработатьВыражение()` так, чтобы семантический анализ использовал данные реестра для стандартных таблиц и исполняемых представлений. Проверка: существующие сценарии разбора и семантической проверки стандартных таблиц и исполняемых представлений проходят без изменения текста запросов.
- [x] 4.5 Проверить влияние на `СемантическийАнализВыраженийПосетитель` и `МодельЗапросаУтилиты`, заменив прямые знания о поставщиках только там, где это требуется для единого разрешения таблиц. Проверка: ссылки на поля, вложенные таблицы, условия соединений, группировки и отборы разрешаются как до рефакторинга.

## 5. Query Constructor UI Integration

- [x] 5.1 Перевести серверную инициализацию формы `КонструкторЗапросов` на создание экземпляра реестра для UI без уничтожения нужной `СхемаЗапроса` до закрытия сценария; клиентский код должен обращаться только к серверным методам. Трассировка: requirement `Query constructor UI uses registry for available table hierarchy`. Проверка: форма открывается и показывает корневые группы поставщиков.
- [x] 5.2 Перевести `КонструкторЗапросовФормы.ЗаполнитьДочерниеЭлементыДереваДоступныхТаблиц()` на ленивое получение дочерних элементов через реестр для стандартных таблиц и исполняемых представлений. Трассировка: requirements `Providers expose a shared metadata contract`, `Registry preserves performance characteristics`. Проверка: раскрытие групп и таблиц не строит все поля всех поставщиков при открытии формы.
- [x] 5.3 Перевести отображение временных таблиц в дереве конструктора на контекстный поставщик реестра, сохранив поддержку вложенных запросов и таблиц значений. Проверка: существующие команды добавления источников и полей из UI продолжают работать.

## 6. Compatibility, Generation, And Reporting Checks

- [x] 6.1 Проверить генерацию текстов запросов и BSL-кода для моделей со стандартной таблицей и исполняемым представлением. Трассировка: proposal impact на генерацию кода. Проверка: сгенерированный текст запроса и BSL-код совместимы с прежним форматом.
- [x] 6.2 Проверить универсальный отчет и СКД-сценарии, которые используют описания источников или исполняемые представления. Трассировка: proposal impact на СКД и универсальный отчет. Проверка: структура источников данных и параметры представлений не меняются для существующих сценариев.
- [x] 6.3 Проверить, что ЗУП-специфичные обработки `Представление*` не получили зависимости от реестра и продолжают предоставлять `Описание()` и `ИмяПредставления()`. Проверка: поиск по измененным обработкам и ручная проверка нескольких представлений.

## 7. Tests And Diagnostics

- [x] 7.1 Добавить или обновить автоматические Vanessa/server-сценарии для выбора поставщика по первому сегменту, неизвестного сегмента, стандартной таблицы и исполняемого представления. Трассировка: all `metadata-provider-registry` requirements. Проверка: feature-сценарий добавлен, YAxUnit-тесты в рамках этой задачи не добавлялись.
- [x] 7.2 Добавить или обновить тесты обработки модели запроса для существующих запросов к стандартным таблицам и `ИсполняемоеПредставление.*`. Проверка: модель источников, поля и параметры совпадают с ожидаемыми.
- [ ] 7.3 Выполнить ручной сценарий в конструкторе запросов: открыть форму, раскрыть стандартные таблицы, раскрыть группу исполняемых представлений, добавить источник и поле. Проверка: UI работает лениво и без ошибок.
- [x] 7.4 Запустить доступную диагностику EDT по проекту `QueryConsoleZUP` и не исправлять unrelated diagnostics в рамках этой задачи. Проверка: новые ошибки, связанные с реестром поставщиков, отсутствуют или зафиксированы.
